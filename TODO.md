# TODO: Fitur Search & Filter Halaman

## 1. Halaman Ticket (Index) — SELESAI

- [x] Perbarui `TicketController::index()`: search, filter status, kategori, jenis, per_page + withQueryString
- [x] Perbarui view `tickets/index.blade.php`: dropdown per-page, kolom pencarian, filter, info data, pagination, fungsi JS

## 2. Halaman Index Category Ticket — SELESAI

- [x] Perbarui `TicketCategoryController::index()`: tambah search & per_page + withQueryString
- [x] Perbarui view `ticket_categories/index.blade.php`

## 3. Halaman Index Ticket Rules / SLA Rules — SELESAI

- [x] Perbarui `TicketRuleController::index()`: tambah search & per_page + withQueryString
- [x] Perbarui view `ticket_rules/index.blade.php`

## 4. Halaman Monitoring SLA — SELESAI

- [x] Perbarui `TicketController::monitoringSla()`: tambah search & per_page + withQueryString
- [x] Perbarui view `tickets/monitoring_sla.blade.php`

## 5. Halaman Daftar User — SELESAI

- [x] Perbarui `UserController::index()`: dukung per_page + withQueryString
- [x] Perbarui view `users/index.blade.php`: dropdown per-page, kolom pencarian, filter role & status gaya Client, info data, pagination, fungsi JS
      </content>
